details:
    type: usersNameFormats
    category: Users
    name: Student Name Formats
    desc: 
    table: gibbonPerson
    modes: { update: true, insert: false }
primaryKey:
    gibbonPersonID
uniqueKeys:
    - username
table:
    username: 
        name: "Username"
        desc: "User account name & login"
        args: {filter: string, required: true}
    studentID: 
        name: "Student ID"
        desc: "Must be unique if set."
        args: {filter: string}
    surname: 
        name: "Surname"
        desc: "Family Name"
        args: {filter: string, required: true}
    firstName: 
        name: "First Name"
        desc: "Given name"
        args: {filter: string, required: true}
    preferredName: 
        name: "Preffered Name"
        desc: "Most common name, alias, nickname, handle, etc"
        args: {filter: string, required: true}
    officialName: 
        name: "Official Name"
        desc: "Full name as shown in ID documents."
        args: {filter: string, required: true}
    middleName: 
        name: "Middle Name"
        desc: ""
        args: {filter: string, required: false}
